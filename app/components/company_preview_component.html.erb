<style>
  .company-preview {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }
</style>

<%= render(BoxComponent.new(color: :secondary)) do |component| %>
  <%= component.body do %>
    <div class="company-preview">
      <%= content %>
    </div>
  <% end %>
<% end %>
