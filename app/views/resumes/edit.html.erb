<center>
<div class="contentblock">
<h1>Editar Currículo</h1>

<%= form_for(@resume) do |f| %>
  
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

<%= f.submit 'Atualizar currículo' %>

<% end %>

<p><%= link_to 'Voltar', root_path %>

</div>